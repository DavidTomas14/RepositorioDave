<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:drawable="@color/white" />

    <!-- Only images are accepted as sources (no XMLs) -->
    <!-- Place image in a density folder, not on 'drawables' or it will not scale properly -->
    <item>
        <bitmap
            android:gravity="center"
            android:src="@drawable/logo_principal_splash" />
    </item>

</layer-list>
